---
title: "Pergunta 1"
author: "Pedro Lobo"
output:
  pdf_document: default
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(comment = "", out.width = "50%", fig.align = "center")
library(dplyr)
library(ggplot2)
library(readxl)
library(tidyr)
theme_set(theme_light(base_size = 16))
```

# Código
```{r, code = readLines("main.r")[-append(l <- grep("library", x <- readLines("main.r")), length(l)+1)], message=FALSE, warning=FALSE, fig.show="hide"}
```

# Gráfico
```{r, code = readLines("main.r")[-append(l <- grep("library", x <- readLines("main.r")), length(l)+1)], message=FALSE, warning=FALSE, echo=FALSE}
```

# Comentários
